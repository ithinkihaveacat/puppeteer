{
  "name": "foo",
  "version": "1.0.0",
  "main": "index.js",
  "description": "foo",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "printenv": "printenv",
    "docker-build": "docker build -t node-${npm_package_name} .",
    "docker-run": "mkdir -p $(pwd)/share && docker run --rm --mount type=bind,source=$(pwd)/share,target=/app/share node-${npm_package_name}"
  },
  "dependencies": {
    "puppeteer": "^1.0.0"
  }
}
